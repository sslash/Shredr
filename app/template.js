/* global define */
define([
    'backbone'
],
function (
    Backbone
){
'use strict';
var Thing = Backbone.Marionette.Thing.extend({

    initialize : function (options) {
    }
});

return Thing;
});
