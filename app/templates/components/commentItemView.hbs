<div class="row clearfix mvl">
    <div class="column ten">
        <img src="/{{user.profileImgFile}}" class="thumb-round img-small">
    </div>
    <div class="column nine-ten">
        <p><small>{{user.username}}<span class="grey-light"> - {{createdAt}}</span></small></p>
        <small class="white">{{body}}</small>
    </div>
</div>
